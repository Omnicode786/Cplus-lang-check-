//create a program that takes two integers from the user and then converts those integer into the ascii code of that integer
#include <stdio.h>
int main() {
    char ch;

    printf("Enter any character: ");
    scanf("%c", &ch);